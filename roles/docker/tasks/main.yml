---- name: 'PREREQUISITES | prepare to install Docker'  include_tasks: prereq.yml  when: inventory_hostname in groups['swarm']  tags: 'docker'- name: 'INSTALL | install Docker'  include_tasks: install.yml  when: inventory_hostname in groups['swarm']  tags: 'docker'- name: 'CONFIGURE | make swarm Docker'  include_tasks: config.yml  when: inventory_hostname in groups['swarm']  tags: 'docker'